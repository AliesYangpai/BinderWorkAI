# —— Gradle ——
.gradle/
build/
*/build/

# —— IDE (Android Studio / IntelliJ) ——
.idea/
*.iml
*.iws
*.ipr

# —— OS 生成文件 ——
.DS_Store
Thumbs.db

# —— 本地敏感配置 ——
local.properties

# —— 签名密钥 ——
*.keystore
*.jks
*.p12
*.key

# —— 日志与临时文件 ——
*.log
*.tmp
*.swp
*~

# —— AIDL 生成代码（可选 — 如不提交 build 目录即可） ——

# —— 其他 ——
captures/
.externalNativeBuild/
.cxx/
